// hw-1
const startButton = document.querySelector('[data-start]');
const stopButton = document.querySelector('[data-stop]');
// hw-2
const startTimeButton = document.querySelector('[data-start]')

export { startButton, stopButton, startTimeButton };
